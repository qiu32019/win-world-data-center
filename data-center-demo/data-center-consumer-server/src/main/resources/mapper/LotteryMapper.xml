<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">    
<mapper namespace="com.ytx.center.server.dao.LotteryDao">

	<!-- 根据key，更新value -->
	<update id="updateIssueById" parameterType="map">
		update lottery set issue = #{newIssue} where id = #{id}
	</update>

	<!-- 根据key，查询value -->
	<!--<select id="queryByKey" parameterType="string" resultType="io.renren.modules.sys.entity.SysConfigEntity">
		select * from sys_config where param_key = #{paramKey}
	</select>-->
	
</mapper>